
<h1><%= @place.name %></h1>

<ul>
<!-- <!% @place.entries.each do |entry| %>
    <li>
      <strong><!%= entry.title %></strong>
      <p><!%= entry.posted_on %></p>
      <p><!%= entry.description %></p>
    </li>
  <!% end %> -->
</ul> 
